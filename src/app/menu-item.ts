export class MenuItem {
  icon: string;
  name: string;
  link: string;

  constructor(icon: string, name: string, link:string) {
    this.icon = icon;
    this.name = name;
    this.link = link;
  }
}
